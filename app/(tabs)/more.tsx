import React from 'react';
import { View } from 'react-native';

export default function MorePlaceholder() {
  // This screen is a placeholder so the "More" tab appears.
  // The actual sidebar is controlled by state in _layout.tsx.
  return <View />;
}
